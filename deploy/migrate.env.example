# Copy beside app.env outside the repository, replace the placeholder, and chmod 600.
# Only the one-shot migrate container receives this owner credential.
MIGRATOR_DATABASE_URL=postgresql://replace-deployment-migrator:replace-migrator-password@postgres:5432/replace-deployment-database
