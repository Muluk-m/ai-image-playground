# apps/web 几乎不再依赖构建期 env — 部署形态和 BFF 接入由 `runtime-config.json`
# 在运行时驱动（schema 见 packages/shared/src/runtime-config.ts；Docker 镜像由
# 仓库根 entrypoint 按 env 模板化写入 dist 根）。
#
# 仅剩两个真正需要 build-time 的 env：

# 默认 API URL（OpenAI 兼容接口）— BYOK profile 创建时的 baseUrl 占位。
# 用户在 UI 里可以覆盖。
# VITE_DEFAULT_API_URL=https://api.openai.com/v1

# 灵感库远程 manifest URL。空字符串禁用远程拉取，仅用 bundle 内置数据。
# 跟着部署走的同源资源（public/inspiration-manifest.json）是默认源。
# VITE_INSPIRATION_MANIFEST_URL=./inspiration-manifest.json
