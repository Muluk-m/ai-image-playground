---
name: product-restyle
description: 何时用：同一张图里换掉商品本身，或改它的颜色 / 材质 / 款式，背景、构图与光影保持不动。不处理：换背景场景（用 scene-swap）、照别人的爆款图重做（用 image-remix）、改图上的文字（用 image-localize）。
---

# 商品换款

> 示例级内容，尚未经过真实产出验证。

一张已经拍好的图，**场景和光影是资产**：布景、机位、打光、投影都不该重来一遍。
要换的只是画面中间那件东西——换成另一件商品，或者换个颜色 / 材质 / 款式。

## 先分清是哪一种

| 用户在说 | 走哪条 |
| --- | --- |
| 「把这里的杯子换成我的保温杯」 | **换主体**：要有目标商品图 |
| 「这款出个黑色的」「换成哑光质感」 | **换颜色 / 材质**：只要原图 |
| 「换成长款的那个版本」 | **换款式**：能给目标图就给，给不了按描述改 |

先逐项识别用户要改的内容；多项要求各自保留，不能只做主要一项。只有「换成什么」存在实质歧义时，
才调澄清工具给 2-4 个具体选项。工具只拟稿，用户编辑提示词并点击「确认生成」后才开始生成。

## 换主体

`editImage`：

- `imageIds` 第一项是**原图**（要改的那张，产出落在它旁边）；后面依次放目标商品的图，
  最多再放三张。同一件商品的多角度图一起给，模型还原细节会准得多。
- 用户提到某件商品却没引用它的图，先 `readLibrary` 按名字查图片 id。
- `n` 保持 1。要多版挑再用 `n`；换成不同商品是不同方案，分别调用。

`prompt` 三句：

1. **换什么**：把目标图中用户指定的 X 替换为参考图里的 Y，明确目标图和参考图的输入序号。
2. **保留什么**：保留用户未要求改变的背景、道具和构图；新商品匹配原商品的位置、大小与朝向，
   仅适配场景所需的透视、光照和接触阴影。
3. **还原什么**：颜色、材质、比例、logo 与包装文字按当前指定参考图还原。用户明确指定某个属性时，
   以用户要求为准；没有明确指定且看不准的颜色或材质，不替它起一个推测的色名或质感名。

产品固有颜色与环境反射分开判断。换了参考图就重新核对，不能沿用上一轮产品的颜色描述。

## 换颜色 / 材质

只要原图，`imageIds` 只填它一张。`prompt` 两句：

1. **改什么**：逐字保留用户指定的颜色或材质；用户说「蓝色」就写「蓝色」，不擅自细化为某种色调或表面处理。
2. **不改什么**：逐项点名——金属扣件、logo、包装文字、商标、背景、光影、材质质感里
   不该变的部分。

守则：**只改颜色就不要顺手改材质，只改材质就不要顺手改款式。** 用户想连主体一起换，
那是上一节；想换背景，那是 `scene-swap`。

## 用选区精确圈定

用户在图上圈了要改的部分，就必须带 `selectionBindings`，逐项抄上图片 id 与选区 id，
`prompt` 写成简短的任务摘要。选区是**编辑边界**，圈里没要求改的东西（文字、背景）照样要保留。
选区框不住要改的对象时，先请用户调整选区，不要自己往外扩。
参考图上的选区表示「参考这一块」，不是「改这一块」。

## 出来不对怎么办

- **颜色或细节不对**：指出候选与用户要求的差异，给出修正提示词供用户检查。
- 只有用户再次点击「确认生成」才重出；拟稿不能触发额外付费任务。
- 默认从原图修正，避免在错误产物上继续累积偏差；用户明确要求沿用某版时使用指定版本。

## 交付

说清三件事：换了什么、保留了什么、这一批是几次生图。
然后点名要他人工确认的地方：logo、包装文字、材质质感、投影方向。
再给 1-2 个具体的下一步：再出一个配色 / 换个场景 / 按这张做主图。
