{
  "name": "image-playground",
  "version": "0.3.5",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.3",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=@image-playground/web",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "deploy:cf": "pnpm --filter=@image-playground/web deploy:cf",
    "deploy:local": "pnpm --filter=@image-playground/web build && launchctl kickstart -k gui/$(id -u)/qlj.image-playground.bff && (launchctl kickstart -k gui/$(id -u)/ai.openclaw.cloudflared-macmini || echo 'cloudflared-macmini agent not present; skipping tunnel restart')",
    "gen:channels": "pnpm --filter=@image-playground/web gen:channels"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "turbo": "^2.5.0"
  }
}
