# Roadmap

产品方向与排期的唯一记录。每项写清「目标 / 现状 / 涉及层 / 待裁决 / 验收」，动工前先走 `/grill-with-docs` → `/to-spec` → `/to-tickets`（见 CLAUDE.md「Spec / Plan 流程」），
这里只登记方向与依赖，不写设计细节。进度勾选在本文件，追踪 issue 见 GitHub `roadmap` 标签。

## 排序原则

按依赖排，不按想到的顺序排。当前约束与例外：

1. **邀请奖励与付费返利不再依赖邮箱验证**。2026-09-16 面谈确认沿用现有注册资格并接受刷号风险，实施规格见 [#414](https://github.com/Muluk-m/ai-image-playground/issues/414)。
2. **智能体、分镜、视频都要扣积分，而现有计费只认「张 × 模型单价」**。计费单元不泛化，三个新模式一个都接不进去。
3. **分镜的产出是视频提示词**，视频生成不先能用，分镜模式没有出口。

推荐顺序：底座（0.x）→ 账号与增长（1.x）→ 视频生成（2.1）→ 智能体（2.2）→ 分镜（2.3）。
0.2 与 1.1 已搁置（见 0.2）；1.2 / 1.3 按 #414 独立推进，1.4 不作为前置。0.1 的图片 / 视频部分已落地。当前主线是 2.1 收尾（Veo）→ 2.2 智能体（含 token 计费单元）→ 2.3 分镜。

## Phase 0：底座

### 0.1 计费单元泛化

- [x] 图片与视频：模型单价带 `unit`（`image` / `second`），预扣接缝收 `quantity × unitMultiplier`，视频按秒计费已落地（公开树 c2de35d，私有树迁移 0006）。
- [ ] 对话：token 计费单元（输入 / 输出 token 单价、按预估预扣按实际结算）随 2.2 智能体一起做，不单独立项。
- 现状：`PrivateTaskHooks.reserveTask` 收 `model + quantity + unitMultiplier`，单价表按 `model` 一行、`unit` 一列。
- 涉及：公开树接缝 `apps/bff/src/lib/private-overlay.ts`、`packages/shared` 的任务协议；私有树 hold / settle / 模型单价表。
- 验收：图片任务行为与流水不变；新增单元种类不改公开树。

### 0.2 邮件基础设施（搁置，2026-09-08）

- 搁置原因：Cloudflare Email Sending 免费计划只能发给已验证地址，发任意收件人要 Workers Paid（5 美元/月），且对大陆邮箱送达无数据；其它服务商暂不接。调研见 `docs/research/cloudflare-email-sending.md`。依赖它的 1.1 继续顺延，解封时按 Resend 免费档起步；1.2 / 1.3 已解除该前置依赖，见 #414。
- [ ] 目标：BFF 能发事务邮件（验证码、密码找回、返利到账、余额不足）。
- 现状：零发信能力；`users` 表只有 `username`（注册时拿邮箱当用户名），没有 `email` / `email_verified_at` 字段。
- 涉及：`packages/db` 迁移、BFF 发信 adapter（服务商经 env 配置，不硬编码）、模板、按邮箱与 IP 的发送限流、能力位 `accounts:email`。
- 待裁决：发信服务商（Resend / SES / SMTP 中转）。
- 验收：没配发信服务商的部署照常跑，只是邮箱相关能力关闭。

## Phase 1：账号与增长

### 1.1 邮箱验证码注册

- [ ] 目标：自助注册必须验证邮箱，验证码 6 位、10 分钟有效、单邮箱单 IP 限频。
- 依赖：0.2。
- 涉及：注册流程改两步（发码 → 验证 + 建号）；`accounts:self-register` 开启时强制；Google OAuth 注册视为已验证。
- 顺带：密码找回走同一套验证码，成本几乎为零，一起做。
- 验收：未验证邮箱不能建号、不发新人积分；一次性邮箱域名黑名单可由运营配置。

### 1.2 邀请注册奖励

- [x] 目标：每个用户一个固定邀请码 / 邀请链接，新用户注册可手填或使用链接预填，双方默认各得 200 分并自动进入可消费积分余额。
- 依赖：现有自助注册与私有计费，不以邮箱验证或新增拉新风控为前置。
- 涉及：注册归因接缝、私有邀请与积分发放、用户邀请页、Admin 奖励策略。
- 口径：双方奖励分别可调，原开户赠送不变；只绑定新用户，不补绑或改绑，只奖励直接邀请人。完整规格见 [#414](https://github.com/Muluk-m/ai-image-playground/issues/414)。
- 验收：注册、邀请关系与双方奖励原子落账，同一注册不重复发放；用户页不披露受邀人身份或实付金额。
- 实现切片：[#418](https://github.com/Muluk-m/ai-image-playground/issues/418)、[#419](https://github.com/Muluk-m/ai-image-playground/issues/419)。

### 1.3 付费返利与积分提现

- [x] 目标：受邀人每笔实付的积分充值、套餐开通及续费，按默认 5% 和 100 积分/元折算返利，逐笔向下取整；返利进入奖励余额，用户手动全额提现为消费积分，不支持现金。
- 依赖：1.2；新增运营确认的实付记录。现有正向积分调整不能直接等同于付款，赠送与补偿不产生付费返利。
- 期限：不设额外邀请关系期限，旧裁决 C2 的半年限制已撤销。
- 涉及：私有实付记录、返利与提现事务、用户邀请页、Admin 实收金额及返利策略配置；不接在线支付网关。
- 验收：同一付款只产生一次返利，重复或并发提现不重复入账，累计奖励不因提现或消费减少；本期不做退款与返利追偿。完整规格见 [#414](https://github.com/Muluk-m/ai-image-playground/issues/414)。
- 实现切片：[#417](https://github.com/Muluk-m/ai-image-playground/issues/417)、[#420](https://github.com/Muluk-m/ai-image-playground/issues/420)、[#421](https://github.com/Muluk-m/ai-image-playground/issues/421)。

### 1.4 拉新风控

- 定位：后续独立规划；#414 本期不新增以下规则，也不以本项作为邀请奖励与返利上线前置。
- [ ] 目标：让 1.2 / 1.3 不被薅。
- 规则起步：同设备 / 同 IP 段注册上限、一次性邮箱域名黑名单、奖励延迟 24h 发放并可撤销、单邀请人日奖励封顶、异常账号一键冻结并回收奖励。
- 涉及：注册与奖励发放路径、Admin 风控面板。
- 验收：每条规则可由运营配置开关与阈值，不硬编码。

## Phase 2：新模式

### 2.1 视频生成

- [ ] 目标：工作台新增视频模式：文生视频、图生视频，结果可预览、下载、进历史。
- 进度：视频模式 composer、feed 与播放器，以及 Grok Imagine / Agnes 两家视频上游已落 main（#189 / #190 / #191）。Grok 续写（`/videos/extensions`）与改视频（`/videos/edits`）已在 BFF 落地。火山方舟 Seedance 2.0（`doubao-seedance-2-0-mini-260615`，最长 15 秒、支持首尾帧）已接入 BFF，靠 `ARK_BASE_URL` / `ARK_API_KEY` 开关。剩余：Veo 上游、按秒或按片计费（依赖 0.1）。
- 现状：BFF 队列模式天然适合分钟级任务，直连 BYOK 路径不适合。
- 服务商分两批：
  - 首发：Grok Imagine 视频、Agnes `agnes-video`、Gemini API 的 Veo 3.x（Google 生视频走 Veo，异步 operation 轮询，需核实当前模型 id 与价格）。
  - 验证效果后接入：火山方舟 Seedance 2.x（字节，C 端即梦同源）。
- 涉及：BFF 上游 style 新增视频协议族、对象存储存视频与封面、任务结果类型扩成 `image | video`、前端 TaskCard / Lightbox 播放、计费按秒或按片（依赖 0.1）。
- 顺序：Grok Imagine → Agnes → Veo（裁决 D1）；视频先只走 BFF 队列不开 BYOK 直连。
- 验收：一条视频任务在三家任一上游跑通并扣费结算；失败按原桶退回。

### 2.2 智能体模式

- [ ] 目标：对话式工作台，智能体能调工具（生图、改图、查素材库、套模板、以后生视频），每轮对话扣积分。
- 依赖：0.1（对话计费单元）。
- 涉及：BFF 新增对话任务类型（流式 SSE）、工具注册表、会话与消息存储、前端会话 UI；LLM 服务商经 channel 配置。
- 口径：对话按 token 折算积分（裁决 E2），每个 LLM 模型维护输入 / 输出 token 单价；工具调用内的生图按现有模型单价计入同一任务。对话历史存服务端（裁决 F1），可跨设备、可审计。
- 验收：一轮对话 = 一条任务占用，按预估 token 预扣、按实际用量结算；余额不足在发送前拦截。

### 2.3 视频分镜与提示词模式

- [ ] 目标：输入一句创意或一段脚本，产出分镜表（镜号、画面、运镜、时长、台词）与每镜可直接投喂视频模型的提示词，一键送 2.1 生成。
- 依赖：2.1（出口）、2.2（它就是智能体的一个专业形态，结构化输出）。
- 涉及：分镜结构化 schema、分镜编辑器（表格 + 参考图）、按目标模型（Seedance / Veo / Grok）的提示词方言、批量送生成。
- 验收：分镜可编辑后逐镜或整体生成；提示词按目标模型切换方言。
- 进展：分镜 schema 与 `POST /api/storyboard/plan` 已落地（能力位 `generation:storyboard`，默认关）；脚本生成本版不计费，只有由它派生的生图与生视频走既有计费。

## 已交付（不在主线上）

- **账户同步**（#176，2026-09-08）：登录用户的模板、素材（含图片本体）与用户设置跨设备同步；本机数据不上云（ADR 0002）。能力位 `accounts:sync`，两项素材配额走 operator-config。剩余素材生命周期缺口见 #222。
- **活跃热力图**（#177，2026-09-08）：按天消耗的一年一屏热力图，账户面板、Admin 用户详情与概览各一张；数据只来自积分流水，私有树实现。

## 候选方向（未排期）

有邮件与积分体系之后成本很低、或对增长有直接帮助的：

- **作品分享页**：单张 / 单条作品公开链接，带邀请码，是拉新的天然入口。
- **每日签到 / 低余额提醒 / 套餐到期提醒**：留存三件套，邮件与站内通知复用 0.2。
- **提示词与模板市场**：把现有「模板」从个人 IndexedDB 升到可发布、可复制，付费模板抽成。地基已就位：模板、素材与用户设置已按账号同步到服务端（#176，2026-09-08 上线）。
- **图片工具箱**：超分放大、抠图、去水印、扩图，作为智能体工具与独立入口，按次计费。
- **付费用户队列优先级**：套餐用户任务排队优先，免费用户限并发。
- **开发者 API Key**：按积分计费的 HTTP API，面向小 B。
- **Admin 报表**：邀请漏斗、返利对账、积分流水导出、模型成本与毛利。
- **国际化**：拉新面向海外时前端多语言与邮件模板多语言。

## 已裁决（2026-09-07；邀请规则于 2026-09-16 更新）

- **A2** 邀请人与受邀人默认各得 200 分，自动进入消费积分余额，双方额度独立可调；取代原双方各 500 分。
- **B1** 充值返利以积分计入奖励余额，用户手动提现为消费积分；注册奖励不经过奖励余额。
- **C2** 每笔符合条件的实付均参与返利，不设额外邀请关系期限；取代原绑定后 6 个月限制。最新规格为 #414。
- **D1** 视频首发顺序 Grok Imagine → Agnes → Veo，之后接 Seedance 2.x。
- **E2** 智能体对话按 token 折算积分。
- **F1** 对话历史存服务端。
