# CONTEXT.md

项目领域与架构词汇表。架构讨论使用的术语（module / interface / depth / seam / adapter / leverage / locality）定义见 `/codebase-design` skill。

## 入口（app mode）

侧栏上的顶层去处，一个入口一个地址、一块主区，切过去必定换掉看见的东西：

- **生图**（`/image`）：纯提示词直出。输入框加参数在这一页，下面只摆最近几条生成。
- **画布**（`/p/<项目>`，没有项目时 `/`）：智能体对话加画布。
- **视频**（`/video`）：把人带到一张预置成视频的画布。
- **作品**（`/works`）：全部生成记录，搜索、状态筛选与批量操作都在这里。

素材、模板、灵感库不是入口，是浮层面板：点开不换地址也不换主区。项目不属于「我的资产」，
它是画布的实例，列在侧栏「最近 · 画布」与素材面板的项目页签里。
_Avoid_: 页面、tab、我的资产（已废）、创作模式 / 作品页（旧名）

## Overlay（浮层）

所有模态浮层共享的深化模块（`apps/web/src/components/Overlay.tsx`）。接口 = 包装组件 `<Overlay onClose tier>{children}</Overlay>`，实现拥有五条纪律：

- **portal 到 `document.body`** — 浮层永远不受祖先 `transform` / `filter` / `backdrop-filter` 包含块影响（2026-08-04 SizePickerModal 事故的根因）
- **scroll-lock** — 组合 `usePreventBackgroundScroll`，内容 ref 自动作为滚动边界
- **ESC 栈** — 组合 `useCloseOnEscape`，一次只关最顶层
- **backdrop 关闭** — 内置 pointerdown-guard（pointerdown 与 click 都必须命中表面本身，才关闭，防划词误关）；暗化层 `pointer-events-none`，否则它盖在表面之上、命中的永远是它，点击永不关闭。需要自定义表面交互（如 Lightbox 的缩放感知点击）的调用方用 `backdrop="none"`，在自己的内容根上挂 handler
- **z 层三档** — `modal`（z-50 基底模态）/ `raised`（z-100 嵌套子弹窗、需压过其它模态的）/ `alert`（z-110 最顶层：ConfirmDialog，以及需要压过 raised 的嵌套浮层）

纪律：模态类浮层一律经 Overlay 渲染内容，不得自己写 `fixed inset-0` + portal。定位型浮层（Tooltip、Select 下拉、拖拽预览）不属于 Overlay，另是一类。

## 槽位（slot）

提示词里 `{名称}` 形状的占位符（名称不含空白与花括号），在输入框里渲染成可点开的 chip。
一个槽位可以填多个值，提交时按各槽位值的笛卡尔积展开成多条已替换的提示词，每条再走现有的
数量分发规则。槽位值是 composer 状态，随「重启保留输入」持久化，不进任务记录。
_Avoid_: 变量、占位符、模板参数

## 引用（mention）

提示词里指向某张参考图的胶囊。**只有一种引用原语**：存的永远是按参考图序号的哨兵标记，
发送时转成 `[image N]`，重排走 remap、被删掉的降级为一段普通文字（**初值文案**，见「界面语言」）。
哨兵标记里的文字（`@图N`）是存储格式，不是界面文案，不随界面语言变；随语言变的只有胶囊的显示标签。选中素材不是第二种语法——
先把素材图按 `imageId` 去重地附加为参考图（已在条里则复用原序号），再插入同一个按序号的引用。
胶囊的显示标签由该图片是否属于某个素材决定（多名同图取最近使用）；**可见文本的长度就是
contentEditable 的光标坐标系**，标签与可见文本不一致光标会整体错位。
_Avoid_: @提及、图片变量、素材引用

## 部署与能力（deployment & capability）

一套代码支撑多种部署，差异用能力表达，不用版本枚举表达。

**能力（capability）**：
`scope:name` 形状的布尔门禁原语，deny by default，由服务端求值。**唯一允许出现在调用点的门禁判据。**
_Avoid_: feature flag、开关、feature、权限

**配额（quota）**：
数值上限。与能力分属两个命名空间——能力回答「有没有这件事」，配额回答「上限是多少」。
_Avoid_: limit、限额开关

**部署形态（deployment shape）**：
一组能力取值的实际组合。是**观察结果，不是配置项**——没有任何地方存储或判断「这是哪种形态」。
_Avoid_: 版本、edition、环境、SKU、双版本

**预设（preset）**：
展开成一组能力默认值的便利名字，仅在配置解析时存在，**展开后即消失**。不得被任何调用点判断。
_Avoid_: EDITION、版本枚举、mode

**私有树（private tree）**：
存在性本身即开关的目录或包。「不在」是默认状态，「在」是例外。
_Avoid_: ee 目录、企业版代码、付费模块

## 身份（identity）

三种身份互不隶属，不要合并。**设备不属于用户**——认证关闭的部署里只有设备、没有用户。

**用户（user）**：
有账号的使用者。只在 `accounts:login` 开启的部署里存在。
_Avoid_: 账号、customer、会员

**自助注册（self-registration）**：
用户自行创建登录账号的能力，由 `accounts:self-register` 单独控制；必须依赖
`accounts:login`，默认关闭。注册后的开户、赠送积分与审计在同一事务内完成。

**邀请注册（invited registration）**：
通过邀请码归因的新用户自助注册。沿用普通自助注册的成功标准，不额外要求邮箱验证或真人核验。

**邀请码（referral code）**：
正常登录用户可自助领取的个人固定分享标识，同时用于分享链接和新用户注册时的可选手动填写。
每个用户只有一个，不支持自定义。

**邀请关系（referral relationship）**：
新用户注册时按最终提交的有效邀请码与一个直接邀请人确立的归属关系；链接只预填邀请码，手动修改可覆盖。
注册后不补绑或改绑，奖励不向更上级邀请人分配。
_Avoid_: 上下级、代理层级、分销链

**设备（device）**：
匿名使用者的浏览器标识，是匿名配额的归属对象。与用户是两套平行身份，不存在从属关系。
_Avoid_: 客户端、访客账号

**运营者（operator）**：
部署与运维这套系统的人，后台的唯一使用者。目前是单一口令，不是用户体系里的一个角色。
_Avoid_: 管理员、admin 用户、超级用户


## 运维（operations）

后台有两个回答不同问题的页面，别混成一个：

**概览（overview）**：
回答「业务跑得怎么样」：任务量、成功率、耗时、失败分布、模型用量。数据全部来自任务记录。
_Avoid_: 大盘、dashboard

**运维看板（operations board）**：
回答「这套部署现在有没有出事」：宿主机还撑不撑得住、各服务活没活、队列有没有卡住、备份有没有断。
它看的是部署本身，不是业务。一个看板只看自己这一套部署。
_Avoid_: 监控页、monitoring、服务大盘

**心跳（heartbeat）**：
一个服务进程定期留下的「我还在，我是哪个版本」的记录。看板据此判断服务死活与线上版本，
不去探测服务的网络端口。
_Avoid_: 健康检查（那是容器编排用的探针，是另一回事）、ping

**宿主机采样（host sample）**：
某一时刻宿主机资源的一次读数：磁盘、内存、CPU、负载、Swap、开机时间，以及同一时刻每个容器的
**容器读数**（内存、CPU、被内核因内存不足杀掉进程的次数）。按时间成序列，只保留近几天，
用来看趋势而不只是当前值。同一台宿主机上的多套部署看到的是同一份读数，容器读数也包括别的部署的容器。
_Avoid_: 指标、metrics（本项目不做通用指标存储）

**接口统计（API statistics）**：
后端每个实例每分钟的请求数、4xx 与 5xx 数、延迟分位数，以及返回 5xx 的接口。只数用户会调到的接口，
不含静态资源、健康检查和内部接口。它回答「后端对用户是否正常」，不是业务成功率——那是概览页的事。
_Avoid_: 访问日志（本项目不存逐条请求）

**部署记录（deployment record）**：
部署脚本每次发布后在宿主机上追加的一行：时间、发布的是哪一套、公开与私有提交、镜像、成功还是失败。
运维看板据此回答「出事前刚发过什么」。

**告警（alert）**：
越过阈值时主动推给运营者的一条消息。只有少数几条「不处理就会出事故」的固定规则，不是可配置的规则引擎。
关于宿主机的告警不依赖应用存活才能发出：磁盘真满的时候，应用多半已经写不动了。
_Avoid_: 通知（notify 在前端指新版本提示）、报警


## 积分与套餐（credits & subscriptions）

经营部署的计费模型只存在于私有树，公开树只提供能力门禁、提交事务 hook 与 UI
插槽。以下术语是私有模块与调用接缝共用的契约：

**积分账户（credit account）**：
每个用户唯一，包含两个余额桶。`subscription`（套餐积分）按月发放并到期清零；
`recharge`（充值积分）长期有效。消费时套餐桶优先，不允许出现负余额。

**积分流水（credit ledger）**：
余额变化的追加式事实记录。预扣、结算、退回、发放、充值和到期清零都必须写流水；
余额是当前状态，流水是解释余额的依据。运营写操作只能经 BFF，Admin 不直接写库。

**注册奖励（registration reward）**：
邀请注册产生、分别发给邀请人与受邀人的额外积分，自动进入双方长期有效的可消费积分余额，不经过奖励余额。
受邀人的普通开户赠送保持独立。
_Avoid_: 开户赠送、充值返利

**充值返利（recharge referral reward）**：
受邀人每笔付费（积分充值、套餐开通或续费）为其直接邀请人产生的奖励，按实际收款金额和返利比例折算为积分。
本期不兑付现金，也不在用户之间转赠。
_Avoid_: 返现、现金佣金、现金提现

**奖励余额（reward balance）**：
已获得、尚未提现的充值返利积分，不能直接消费；不包含自动到账的注册奖励。

**积分提现（reward transfer）**：
用户手动将全部奖励余额转为自己长期有效的可消费积分，不涉及现金兑付。
正余额即可提现，无额外门槛、审核或手续费。
_Avoid_: 现金提现、返现

**累计奖励（total rewards）**：
本人获得的注册奖励与充值返利积分之和，不包含普通开户赠送；提现或消费不会减少累计奖励。

**邀请奖励策略（referral reward policy）**：
全站统一的双方注册奖励额度、充值返利比例与返利折算率；双方注册奖励和充值返利可分别关闭，不区分邀请人。

**返利折算率（reward conversion rate）**：
将按货币金额计算的返利换算成奖励积分的比例，独立于套餐权益和当次购买的积分数量。

**实付记录（confirmed payment）**：
运营确认的一次实际收款事实，关联积分充值或套餐购买；套餐后续按月发放积分不构成新的收款。

**任务占用（task hold）**：
提交事务内按 `task_id` 唯一创建的积分预扣。任务交付了图片（`completed`）才结算；`failed`
与 `cancelled` 一律按原桶退回，不看上游被调用了几次。重试只更新真实上游调用次数，不重复预扣。

**模型单价（model price）**：
模型每个计价单位所需的积分数，单位是每张图片、每秒视频或每千输入 token。一次提交的价格是
`单价 × 数量 × 单位倍率`；缺少有效价格时拒绝提交。对话模型那一行另存每千输出 token 的积分数
与一轮预扣的输出 token 上限，运营在后台改，改完下一轮就生效。

**消耗（spend）**：
一个已交付任务实际扣掉的积分，等于它的任务占用额；失败与取消的任务退回后消耗为零。归属日取结算
那一刻，日界按部署时区（运营配置）切，不按用户浏览器。
_Avoid_: 用量、扣费、花费、活跃度

**活跃热力图（activity heat graph）**：
按天的消耗格子图，一年一屏，用户看自己的、运营看单个用户与全站。数据只来自积分流水，不另立
统计表；没有积分的部署没有这张图。
_Avoid_: 贡献图、日历图、热图

**套餐（subscription plan）**：
定义每月积分和 1 / 3 / 12 个月价格。生效订阅记录起止时间与下一次月度发放时间；月度发放
由读路径惰性核对和后台维护扫描共同保证幂等。

## 素材库（library）

**素材（asset）**：
给一张已存图片起的名字，记录 `{ id, name, imageId, createdAt, updatedAt, lastUsedAt }`，存在主 IndexedDB 的
`assets` 表里，跟随 scope 隔离与匿名库领养。图片本体与缩略图仍归 image store，同一个 `imageId`
可以有多条素材记录，删素材不动图片。
_Avoid_: 图库、收藏夹、素材图

**模板（template）**：
一段可复用的提示词连同它引用的素材与参数快照，记录 `{ id, name, prompt, assetIds, params,
createdAt, updatedAt, lastUsedAt }`，存在主 IndexedDB 的 `templates` 表里。`prompt` 存带哨兵标记的形式，
`assetIds` 按引用序号排列（那一位不是素材就记 null）。套用先补齐缺席的素材图，再按新顺序
remap 引用——素材已删的位降级为「已移除」，套用仍然成功。
_Avoid_: 预设、快捷短语、prompt 片段

## 用户设置与同步（user settings & sync）

**用户设置（user settings）**：
可跨设备携带、不含密钥的设置：行为开关、默认生成参数、当前选中的内置 channel
与模型、置顶灵感、各种「引导已关闭」标记。作为一份整体存取，不做字段级合并。
当前页面属于会话导航，不属于用户设置；与商品图任务上的「偏好」也无关。
界面语言与主题是**显示设置**，同样不属于用户设置：它们登录前就要生效，而且一台设备一个样。
_Avoid_: 偏好、配置、账号配置、settings 大杂烩

**同步（sync）**：
登录用户的模板、素材（含图片本体）与用户设置在设备与服务端之间的合并，只在 `accounts:sync` 开启的
部署存在。本机与服务端各自的记录取并集，同一条记录以最后写入为准，删除以墓碑传播；素材记录只有
在图片本体已上传后才算可同步。服务端这份是用户档案，长期保存，不随任务记录过期。
素材与模板两张表为此常备两件事，与同步是否开启无关：`updatedAt` 只在内容改动时前进（「使用」只动
`lastUsedAt`），删除写成只留 id 与时间戳的**墓碑**行，所有读路径过滤掉它。
_Avoid_: 备份、云存储、上传、账号存储

**本机数据（device-local data）**：
普通任务记录、商品图任务、未迁移的本地项目、composer 草稿与槽位值，只存在于当前浏览器。云端项目已确认的画布结构属于项目同步集合，未上传媒体仍是本机数据。
_Avoid_: 生成记录、历史（口语，边界不清）

## 商品图（product shots）

商品图工作流（复刻套图向导 #132、换背景与换产品、抠图）已整体下线（#643）；以下术语只用来读懂浏览器本地的旧记录：
`bgswap_jobs` 仍被作品页读来给旧任务命名、被图片保留判断读来保住引用的图，`remix_sets` 表保留不读。

**商品图任务（product shot job）**：
一组原图，连同任务级的产品素材、一句偏好与每张要出几版，作为一个整体跑完。原图来自上传、竞品
链接或素材库；任务名来自链接标题，没有就是「商品图 N」。记录 `{ id, name, images, preference,
versionsPerImage, mode, productAssets, createdAt, updatedAt }` 存在主 IndexedDB 的 `bgswap_jobs`
表里（表名是历史，不是概念），跟随 scope 隔离与匿名库领养；每张图记
`{ imageId, sourceUrl?, sceneType?, sourceMatte?, versions, chosenVersionId? }`。
_Avoid_: 套、批次、换背景任务、任务组

**动作（action）**：
对原图执行的变换：`background`（原图产品，只换背景）、`replace-product`（换成素材里的产品，
背景像素不动）、`replace-and-background`（两样都换）；动作决定重绘哪一侧、使用哪些参考图与提示词，
产品素材整任务生效，最近一次动作供批量沿用。
开始派生遮罩时选定的原图蒙版是本次动作的固定快照，后续手改只影响之后的动作，晚到的计算不得
覆盖更新后的原图蒙版。
_Avoid_: 模式、bgswap mode、镜头类型

**产品清单（product inventory）**：
原图里算作产品、换背景时一个像素都不该动的那组东西，是一份可枚举的部件名（如「浴缸、落地龙头」）。
判据是**功能归属**而不是物理相连：产品本体，加上功能上属于这件商品、拿走商品就没有存在意义的部件，
接不接触本体都算（落地龙头算、台灯电源线算）；独立摆放的陈设不算（手机支架、沙发抱枕、咖啡机旁的
咖啡杯都不算）。清单由视觉模型在出方案时列出，偏好里点名要保留的东西强制并入、压过模型自己的判断。
清单是语义层的边界，与像素层的 `productBox` / 蒙版不是一回事；**不用关键词表定义**，任何写死的
品类词都是反模式。
_Avoid_: 附件、配件、产品框、保留区、关键词表

**道具（prop）**：
方案句允许写进新环境的物件：可独立拿走、不在产品清单内、且不与清单中任何一件功能重复。
「产品已有龙头、方案再点缀一个龙头」违反的是最后一条。
_Avoid_: 配件、陈设、装饰

**偏好（preference）**：
任务级的一句自由文本，实际承载两件事：**保留项**（用户点名不能动的东西，由视觉模型抽出并入产品清单）与
**风格愿望**。界面上不拆字段，提示词末尾附的仍是整句原文，抽取只是多了一条进清单的路。
_Avoid_: 风格要求、备注、附加提示词

**版本（version）**：
一次动作的产出，属于商品图任务中的某张原图，保留实际提交的提示词、蒙版与目标图快照，不因
后续手改而追改；同一张原图的多版都保留，用户选一版定稿，重跑沿用本版动作。
版本不拥有独立进度，进度由关联的任务记录派生，任务摘要上的动作标签取各版本动作的去重集合。
_Avoid_: 镜头、结果、出图记录

**方案抽屉（plan drawer）**：
某一版的简报与提示词摊开可改的浮层，版本条与总览版本卡各有一个入口，默认收起。改简报字段就地重算
提示词（借创意重做走六段构建器，其余动作走共用的换背景模板）；提示词一旦被人手写过，这一版就标
「手改」，简报改动不再覆盖它，只有「重置为 AI 版本」能收回标记。「按此重生成」拿抽屉里的提示词另
起一版，遮罩与参考图照原动作再走一遍，新版一律算手改。图上文案的语言是任务级设定，摆在抽屉顶部。
_Avoid_: 编辑提示词面板、提示词覆盖、镜头编辑

**导出（export）**：
平台预设（`amazon` / `alibaba` / `pinduoduo` / `site` 的尺寸）与裁切 / 留白只在总览的导出面板出现，
流程里不选平台。范围默认「选用版」，一版都没选时导出全部。
_Avoid_: 平台档位、尺寸设置、导出预设当任务设置

**画面类型（scene type）**：
一张原图是 `photo`（产品实拍）、`infographic`（示意图）、`callout`（带标注的特写）还是
`collage`（卖点拼图），由视觉模型的预检给出，记在原图上。非 `photo` 的图带说明文字，换背景
会把它们毁掉：批量默认跳过，单张要用户确认。认不出类型就当 `photo`。
_Avoid_: 场景、图片分类、sceneType 自由文本

**原图蒙版（source matte）**：
属于某个商品图任务中的原图而非某一版本的抠图结果，原图入任务时取得并供动作复用；**alpha 是唯一
权威**，保留抠图来源、预览与对应的原图，不保存派生遮罩，每次动作的遮罩、附件回捞与蒙版一致性
按其选定的 alpha 和产品框派生，手改过的 alpha 不再回捞或校验。
切换任务不终止尚未完成的抠图，结果只归发起它的原图，原任务或原图已删除时结果作废；未完成的
抠图状态不属于任务记录。
原图、alpha、目标图和预览属于原图蒙版，版本遮罩属于版本；它们不因普通生成记录被删除而失去所有者。
「蒙版丢失」指记录还在、alpha 图片却已不存在，不是抠图模型运行失败；重新抠图不能视为恢复原有手改。
_Avoid_: 版本蒙版、遮罩缓存、maskImageId（那是版本上的快照）

**蒙版一致性（matte agreement）**：
原图 alpha 的外接框与方案给的 `productBox` 的重叠率，每个动作按它自己那个框现算。低于阈值
判为抠错了对象，这一版不带蒙版提交并标「蒙版不可靠」——占比正常不等于抠对了东西。结论会回写
到原图上供状态标签显示，但只是缓存：谁也不许拿它当派生依据。
_Avoid_: 蒙版质量、抠图置信度

## 分镜（storyboard）

**分镜（storyboard）**：
旧导演台的镜头表，连同导演台、分镜规划接口与 `generation:storyboard` 能力一起下线。浏览器本地的旧记录
只读保留、不迁移：作品页用它的标题给分镜出的那一套图起名，图片保留判断照常把它引用的图算进去。
「视频」入口（`/video`）现在是自己的一页：它只负责把人带到一张预置成视频的画布（新建或上次那张），
片段编排与成片仍在画布的时间线里。
_Avoid_: 导演台（已下线）、脚本、故事板

## 智能体（agent mode）

创作模式画布旁的对话面板，智能体调工具出图、改图、出视频，产出落画布。以下术语是对话、计费与画布三方共用的契约。

**会话（conversation）**：
一段与智能体的连续对话，归属一个用户或一个设备；设备登录后领养到用户名下，之后不再按设备查。
标题由首轮消息自动生成，不可改名。
_Avoid_: thread、聊天、chat session、话题

**轮（turn）**：
从用户一条消息到智能体回复结束的整段，中间可有零到多次工具调用。轮是消耗展示与对话计费的单位。
失败的轮不留那段没收尾的回复——面板当场撤掉它，历史里也没有它；中止的轮保留，那段话用户还看得见。
_Avoid_: run、消息对、请求、step

**对话任务（chat task）**：
一轮对话在任务体系里的占用记录：按预估 token 预扣，按上游返回的实际用量结算，退回差额。工具里的生图 /
生视频是各自独立的任务，只用会话与轮关联，不并入对话任务——失败退回的语义各走各的。
_Avoid_: 把工具任务算进对话任务、按轮固定价

**工具（tool）**：
智能体可以调用的一项能力。首批四个：生图、改图、读素材库、生视频；后来多了一个读取技能。
生视频只在开了 `generation:video` 的部署里、且这一轮是视频轮时进模型的工具清单。工具的产出放在
画布上被引用的源图旁边，源图不动；对话流里只留缩略卡片。
_Avoid_: 用「技能」指代工具、function、插件、能力（与 capability 冲突）

**技能（Agent Skill）**：
一份写在 `SKILL.md` 里的流程指引，按 agentskills.io 的目录约定随 BFF 镜像发布，运营改文件即可上线，
不用改代码。**技能不是工具**：工具是模型能调用的动作；生成工具先拟提示词，确认后才创建付费任务。技能只是一段
告诉模型「这类活该怎么干」的文字，读进来不产生任何产物。模型常驻看到的只有每条技能的标识与
「何时用」，正文要它自己调读取技能工具才进上下文。用户也可以用 `/技能标识` 显式点名一条。
技能有两个名字：**标识**是 kebab-case 的 `name`，服务端与 `/` 命令只认它；**标题**取正文第一个
一级标题，只给人看（`/` 菜单、面板上那行「读取技能：…」），不进模型的上下文。
_Avoid_: 把技能叫成工具、prompt 模板、预设、插件、把标识显示给用户

**创作类型（mode）**：
一轮要做的是图片还是视频。它只决定这一轮装配给模型的东西——工具清单、技能清单、系统提示词那一句，
不决定历史怎么渲染：旧轮的生视频结果在图片轮也照样认得出来、照样出卡。请求里缺席即图片。它不落库，
下一轮由客户端再说一次。
_Avoid_: 模式（与创作模式/工作台模式冲突）、把 mode 当会话属性、按 mode 过滤历史

**产物（artifact）**：
一次工具调用产出的一件东西，带介质判别：图片或视频。`artifactId` 同时是画布对象的 id，结果卡与画布
凭它指同一个东西。视频产物落画布的是封面，点封面就地播放，画布上不存 mp4。
_Avoid_: 图片（视频也是产物）、输出、附件

**产物交付（artifact delivery）**：
已生成的产物在本机成为可定位画布对象的过程，归属发起它的轮与画布，完成时间独立于工具生成和本轮消耗。
产物落进用户确认生成后在画布上占好的位，用户中途编辑画布不会让它拒写。原画布已离开、会话已切换或本机
取图失败时，产物留在结果卡中供手动放入，不追写新目标。手动放入的入口只从「这张产物此刻在不在画布上」
反查，不看交付时留下的标记，所以刷新页面、甚至把对象从画布上删掉之后，入口都还在。结果卡上的图：
在画布上的那些由画布渲缩略图（画布仍是这些位图的单源），不在画布上的回退到服务端按 `taskId` 取原图
（视频取封面），所以没落下去的产出刷新后仍看得见。
_Avoid_: 生成完成、工具状态、下载完成

**排队消息（queued message）**：
智能体忙时用户发出的消息，默认排在当前这段回复之后依次处理。它存在服务端，刷新页面、换设备都还在，
输入框上方列出来，每条可以撤回；撤回和被处理之间只有一个结果成立。用户按停止时，排队消息回到输入框，
由用户决定改了再发还是删掉。每个会话最多排 10 条。澄清的答复不排队尾，插到队首先处理。
_Avoid_: 待发消息、草稿（草稿是没发出去的）

**插话（interjection）**：
把一条消息插进智能体正在做的事里，在它两个动作之间生效，会影响手头的活。排队消息可以就地升级为插话。
_Avoid_: 打断（打断是停止）、steer（实现用词）

**生成草稿（generation draft）**：
智能体为一次图片生成、改图或视频生成准备的完整提示词与输入材料，还不是后台任务。用户可先修改提示词，
点击「确认生成」后才提交并预扣生成积分；对话拟稿本身仍照常计费。确认不让模型再次改写提示词。
原始生成草稿随会话保存；尚未确认的本机文字修改不跨刷新保存。重复确认同一份草稿只创建一个任务。
_Avoid_: 已提交任务、已开始生成、对话输入草稿

**后台任务（background job）**：
用户确认生成草稿后提交的一次生图或生视频，不阻塞智能体继续说话或处理下一条消息。任务结果按
产物交付落到画布；任务有自己的状态、消耗和取消入口。停止对话不取消已提交的后台任务，删除会话才一并取消。
_Avoid_: 子 Agent（它不调用对话模型）、工具任务、异步工具

**唤醒（wake）**：
后台任务结束后让智能体再起一轮去看结果。成功时要不要唤醒由智能体在提交任务时决定；智能体提交的任务失败一律唤醒，
让智能体向用户说明并提议下一步；用户自己点的重试失败不唤醒，用户当场就看得到。没有唤醒的结果，智能体在用户下次说话时自然看到。

同一次提交的任务全部结束才唤醒一次，等太久就带着已结束的先唤醒。唤醒时恰好有排队消息，就先处理那条
消息，并把任务结果一起带进这一轮，不再单独唤醒。用户没说话时连续自动唤醒有上限，到了就停下等用户。
积分不够时不唤醒，结果照常落画布，面板上说明智能体没有查看。每次唤醒都是一轮，照常计费。
_Avoid_: 回调、通知（通知是给用户的）

**中断续跑（interrupted turn resume）**：
服务重启打断了一段正在进行的回复时，丢掉说了一半的那段，告诉智能体上一轮被中断，让它接着做。同一段被
打断的回复只自动续一次（续上的那一轮再被打断也不再续）；已提交的后台任务不受影响，也不会重复提交。
_Avoid_: 重试（重试针对失败的工具调用）、断点续传

**失败占位（failed placeholder）**：
用户确认生成后在画布上占好的位，任务失败后留下的出错框。它和结果卡指同一次生成，刷新页面后仍在，
是重试的入口之一。
_Avoid_: 错误图片、坏图、失败产物（失败没有产物）

**重试（retry）**：
把一次失败的工具调用按模型当时选定的参数原样再执行一次，不经过对话模型重新决策。重试以一个失败占位
为单位：只重出这一张，结果落回这个占位；对话里那张失败卡可以一次补齐它剩下的全部失败占位。重试按
当时的价格扣费，按钮上先写明预估积分，不另弹确认；当时用的模型已下线时不能重试。每次重试在对话末尾
按时间追加一条重试记录，挂新的结果卡并能跳回原来那张失败卡；原失败卡保持失败，不改写历史。
重试不调用对话模型，只执行那一次工具调用，有自己的消耗，可以中止并退款。重试进服务端的队列依次执行，
刷新页面或换设备后排队状态仍在；排队中的项可以单独取消，取消后恢复成失败占位。轮到的重试因积分不足失败时，
队列里剩下的重试全部撤回成失败占位。

只有重跑有望成功的失败才能重试：上游出错、超时、没出图。积分或额度不够时引导去充值，未登录时引导去登录，
参数本身出错时交给智能体重新处理。局部改图和连锁改图的后续步骤依赖那一轮的临时状态，不能原样重试，
同样交给智能体。开了云端同步的项目也保留失败占位，跨设备可见、可重试。

想换方向不叫重试，是在对话里说一句新的话。
_Avoid_: 重新生成（泛指新一轮）、再来一次、重发消息

**本轮消耗（turn cost）**：
一轮里对话 token 折算的积分与该轮工具任务积分的合计，界面上是一个数，点开看明细。失败的轮不收费，
并在回复里明说没有扣费；没有工具调用的纯对话轮也照 token 计。上下文压缩产生的 token 不计入任何一轮。
_Avoid_: token 数当主展示、费用、账单

**调用用量（model call usage）**：
智能体每次模型请求的独立事实，绑定会话、轮和执行身份；不随工作消息淘汰。对话调用参与本轮
原有结算，摘要由平台承担；图片和视频工具仍各走独立任务。未知用量不等于零，详见
[调用用量记录](docs/agent-usage.md)。
_Avoid_: 积分流水、每条回复计费、窗口账单

**上下文压缩（compaction）**：
送给模型的输入超过阈值时，用摘要替换旧轮、保留最近若干条原文的塑形。只影响模型输入，存储里的消息
一条不改，用户翻历史看到的是全量。摘要带锚点（覆盖到哪条、覆盖几条）：锚点吻合直接复用，只新增了
消息就增量折叠，失配或折叠满次数就全量重做；连续失败则熔断一段时间，熔断期仍可复用旧摘要。
_Avoid_: 截断、清空历史、删旧消息、上下文窗口（那是模型参数）

**澄清（clarification）**：
智能体在一轮里向用户发出的单选提问；用户的选择回填为下一条用户消息，这一轮到此结束。
_Avoid_: HITL、审批、中断、表单

**授权原文（authorization text）**：
本轮用户许可的修改范围：末尾那条未完成澄清链上的用户原话与澄清摘要，收尾是本轮消息与它的引用清单。
用户插话会把那句话追加进来并换一个版本；模型摘录的 requestQuote 必须是它的子串。它与送给模型的输入
读同一段历史，但已完成的轮只进后者。
_Avoid_: 执行原文、修改要求、edit request

对话输入框里的图片引用沿用「引用」原语：画布对象和素材都先附加为参考图，再插按序号的引用。
图片和遮罩以引用时的状态属于会话，澄清、续聊和重新打开会话都不清空；编号指最近一批引用，
更早的图仍保留原身份，不会因新一批出现相同编号而被替换。

## 界面语言与显示（locale & display）

屏幕上给人看的语言与明暗。它只管人读到、看到什么，不管模型收到什么。

**界面文案**：
渲染给人看、随界面语言切换的文字。它只有一个去处：屏幕。
_Avoid_: 文本、字符串、copy

**模型输入**：
拼进提示词发给上游模型的文字，既包括提示词模板，也包括随请求带上的枚举值（分镜风格、镜头运动、方向词）。
它不随界面语言变；界面要展示它时另取一份只管显示的标签，值本身原样保留。
_Avoid_: 提示词文案、prompt 文案

**显示设置**：
只影响这台设备上怎么显示、登录前就生效的设置：界面语言与主题（亮、暗）。没选过时跟随环境（浏览器语言、系统明暗），
选了就固定在当前浏览器。它不属于用户设置，不随账号同步。
_Avoid_: 偏好、外观设置、个性化

**初值文案**：
写入那一刻按界面语言取值、写完即成用户数据的文字：新建配置的名字、复制配置的后缀、默认任务名、引用被移除后留下的占位。
此后不随界面语言变，已经写下的也不回头迁移。
_Avoid_: 默认文案、占位文案

## 测试

- `apps/web` 有 jsdom 环境（按文件 `@vitest-environment jsdom` 启用），组件级冒烟测试的入口；Overlay 是首个有 DOM 锚点的模块。

## 参数策略（paramCompatibility）

「这个 profile 下哪些参数可用 / 合法」的唯一权威模块（`apps/web/src/lib/paramCompatibility.ts`）：

- **`getParamCapabilities(profile, outputFormat)`** — 能力查询。UI chip 显隐（ParamControls / InputBar）与归一化共用同一判定，禁止在组件里重新推导（codexCli 或 channel 未声明 quality capability→无 quality、gemini/非 png→无透明输出、png→无压缩、responses→无审核）
- **`normalizeParamsForSettings(params, settings)`** — 归一化，幂等。在分发层（`callImageApi` / `resumeQueueImageApi`）**强制执行**：任何提交路径（工作台 / canvas / 恢复 / 重试）到达 adapter 前必过，调用方不需要自觉
- store 提交路径与 InputBar effect 里的归一化调用是另一层职责（任务记录保真 + UI 回写），不是第二套约定

## 项目与画布（project & canvas）

**项目（project）**：
围绕同一创作目标的一张画布及其会话归属，有独立且稳定的身份；名称变化不改变身份。云端项目归属同一部署中的一个登录用户，本地项目只在当前设备存在。
_Avoid_: 会话 ID 当项目身份、文件夹

**项目画布（project canvas）**：
项目里的有序对象和标注，以及它们的位置、尺寸与外观。相机、选区、工具和撤销历史是设备的编辑状态。
_Avoid_: 会话即画布键、模型上下文

**项目修订（project revision）**：
服务端确认的一次项目结构或名称变更的版本，用于识别另一设备已经提交的更新。它不等于浏览器编辑次数、消息序号或模型执行窗口。
_Avoid_: 最后写入胜出、时间戳版本

**项目同步冲突（project sync conflict）**：
本机编辑基于较早的项目修订，另一设备已提交更新；两份内容都需保留，不可静默覆盖。它是设备之间的边界，与产物交付无关：产物落画布不看用户改没改过画布。
_Avoid_: 网络错误、生成失败

**画布已同步（canvas synchronized）**：
当前画布结构及必要媒体已获云端确认。它不表示未发送草稿、会话关联或尚未完成的生成产物也已同步。

**持久媒体（durable media）**：
用户拥有的稳定媒体身份、原件与预览，容量预留、上传确认和项目引用共同决定是否已保存。它位于临时任务过期规则之外；访问地址在鉴权后短期签发，不是场景数据。详见 [ADR-0008](docs/adr/0008-durable-project-media.md)。


## 创作云同步（creation cloud sync）

平台执行的生成任务属于登录用户，创作记录与临时执行任务分开保留。浏览器直连生成由用户选择上传，API Key 不进入同步数据。完整范围与原件保留决定见 [ADR-0007](docs/adr/0007-durable-generation-cloud-ownership.md) 和 [#483](https://github.com/Muluk-m/ai-image-playground/issues/483)。

生成档案（generation record）使用稳定任务身份；命令回执（generation command receipt）保存请求摘要和结果身份，执行任务过期不删除回执。一个事务内的创作变化组成持久批次，按用户事务计数器排序；计数器锁在业务与计费锁之后获取并持有到提交。临时游标、选区、相机和撤销栈不在云同步范围内。

**项目产物预留（project output reservation）**：云端图片任务接受时，任务、原项目、会话、轮及产物位置在同一事务内确定；画布中的生成占位使用稳定产物身份。worker 归档只替换仍存在的对应占位并保留当前几何，递增项目修订；取消或没有可交付原件的占位被收回。删除占位不删除创作原件，也不允许旧快照复活该占位；用户仍可明确从创作记录放入图片。浏览器自动交付只刷新云端项目，不再次插图。项目预留先于账户变更发布，生成档案外键延迟到事务提交检查。

**生成来源（generation source）**：真正的图片工具任务与普通图片生成共用生成档案；对话轮不是图片生成记录。档案首次发布时冻结原会话、轮与项目身份，独立会话的项目为空，旧档案未知来源保持为空。执行任务和会话后续清理不改写该来源。历史封面、详情输出、会话产物和项目对象共享稳定产物身份；明确从历史放入当前项目可恢复已删除的对象，重复放入只定位已有对象，读取原件期间切项目则取消此次放置。

**平台记录镜像（generation mirror）**：作品页只有一种卡。只在平台留有记录的生成（智能体出的图就属于这种）
被镜像成一条普通的本机任务记录，产出用 `aip-media:<mediaId>` 引用——像素存在哪儿不是用户要感知的事。
复用、编辑、放入画布、重试这些要真实像素的动作，在动手前把它搬回本机（下原图、换成本机图片 id、
认出它用的内置模型）；此后它与本机生成完全同构，下游不再分情况。删除这种记录走平台软删
（`DELETE /api/generations/:id`），平台没删掉就不动本机列表——本机抹掉刷新就长回来，不算删。


**项目回收（project recycling）**：云端项目删除保留不可重用的身份墓碑，默认 30 天回收窗口由 `sync:project-recycle-days` 配置。回收内容仍占容量，原会话随项目隐藏/恢复；删除与恢复都递增修订并清除旧命令回执，陈旧写入不能复活原项目。目录携带已删身份以移除其他设备列表，本机草稿和场景不销毁，可从回收站明确另存为新身份。过期后停止恢复；物理媒体回收与引用解除由 #440 后续交付。
